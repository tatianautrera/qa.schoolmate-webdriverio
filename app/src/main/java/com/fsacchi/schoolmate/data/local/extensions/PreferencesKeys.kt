package com.fsacchi.schoolmate.data.local.extensions

enum class PreferencesKeys {
    USER_EMAIL,
    USER_PASS,
    SAVE_CREDENTIALS,
    ALLOW_RECEIVE_NOTIFICATIONS,
    DAYS_UNTIL_JOB,
    DAYS_UNTIL_TEST,
    DAYS_UNTIL_HOMEWORK
}